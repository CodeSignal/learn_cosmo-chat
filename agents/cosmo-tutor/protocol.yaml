input:
  EXTRA_INSTRUCTIONS:
    type: string
    optional: true
    default: ""
    description: Additional instructions appended to the system prompt at session creation.

triggers:
  user-message:
    input:
      USER_MESSAGE:
        type: string
      FILES:
        type: file[]
        optional: true


skills:
  crawl-urls:
    display: description

agent:
  model: anthropic/claude-opus-4-7
  system: system
  input:
    EXTRA_INSTRUCTIONS: EXTRA_INSTRUCTIONS
  temperature: 0.7
  webSearch: true
  agentic: true
  imageModel: openai/gpt-image-1
  skills:
    - crawl-urls

handlers:
  user-message:
    Add user message:
      block: add-message
      role: user
      prompt: user-message
      input: [USER_MESSAGE, FILES]
      files: [FILES]
      uiContent: USER_MESSAGE
      display: hidden

    Respond:
      block: next-message

